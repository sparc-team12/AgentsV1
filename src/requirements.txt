# Runtime dependencies for the Lambda package.
# boto3 is provided by the Lambda runtime and is deliberately not vendored.
fastapi==0.115.6
pydantic==2.10.4
mangum==0.19.0
# Only loaded if PHRASING_BACKEND=gemini is set (chat_agent._phrase_gemini);
# omit from the zip if that backend is never enabled for this deployment.
google-genai==1.3.0
